apiVersion: v1
kind: Service
metadata:
  name: {{ include "monsterchart.fullname" . }}
  labels:
    {{- include "monsterchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monsterchart.selectorLabels" . | nindent 4 }}
