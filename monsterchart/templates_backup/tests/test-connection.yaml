apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monsterchart.fullname" . }}-test-connection"
  labels:
    {{- include "monsterchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "monsterchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
