apiVersion: v1
kind: Service
metadata:
  name: imagebackend-{{ include "monsterchart.fullname" . }}
  labels:
    app: monsterstack
spec:
  ports:
    - port: 8080
  selector:
    app: monsterstack 
    partie: imagebackend-{{ include "monsterchart.fullname" . }}
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: imagebackend-{{ include "monsterchart.fullname" . }} 
  labels:
    app: monsterstack
spec:
  selector:
    matchLabels:
      app: monsterstack
      partie: imagebackend-{{ include "monsterchart.fullname" . }}
  strategy:
    type: Recreate
  replicas: 5
  template:
    metadata:
      labels:
        app: monsterstack
        partie: imagebackend-{{ include "monsterchart.fullname" . }}
    spec:
      containers:
      - image: "{{ .Values.imagebackend.image.repository }}:{{ .Values.imagebackend.image.tag }}"
        name: imagebackend
        ports:
        - containerPort: 8080
          name: imagebackend