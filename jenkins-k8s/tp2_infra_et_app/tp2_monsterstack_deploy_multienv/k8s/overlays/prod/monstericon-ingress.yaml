apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: monster-ingress
  # annotations:
    # nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: monstericon.v3s2.dopl.uk 
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: monstericon
                port:
                  number: 9090
